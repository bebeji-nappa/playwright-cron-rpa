{
  "name": "playwright-cron-rpa",
  "packageManager": "yarn@3.2.0",
  "scripts": {
    "rpa": "yarn playwright test --headed",
    "rpa:record": "yarn playwright codegen"
  },
  "devDependencies": {
    "@playwright/test": "^1.21.0",
    "@types/node": "^17.0.24",
    "dotenv": "^16.0.0",
    "ts-node": "^10.7.0"
  },
  "dependencies": {
    "@types/node-cron": "^3.0.1",
    "child-process": "^1.0.2",
    "node-cron": "^3.0.0",
    "typescript": "^4.6.3"
  }
}
